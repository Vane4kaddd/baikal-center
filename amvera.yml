meta:
  environment: python
  toolchain:
    name: pip

build:
  buildCommand: pip install -r requirements.txt

run:
  command: python app.py
  containerPort: 5000

persistentPaths:
  - /data
  - /data/uploads
  - /data/instance
