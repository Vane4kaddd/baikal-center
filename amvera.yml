meta:
  version: 1.0
  name: baikal-center
  toolchain:
    name: python
    version: "3.11"

build:
  commands:
    - pip install -r requirements.txt

run:
  command: python app.py
  port: 5000

persistentPaths:
  - /data
  - /data/uploads
  - /data/instance
