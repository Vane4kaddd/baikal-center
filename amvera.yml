meta:
  environment: python
  toolchain:
    name: pip

build:
  command: pip install -r requirements.txt

run:
  command: gunicorn --bind 0.0.0.0:5000 app:app
  containerPort: 5000
  persistentPaths:
    - /data
    - /data/static/uploads
    - /data/instance