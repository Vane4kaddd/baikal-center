meta:
  version: 1.0
  name: baikal-center
  toolchain:
    name: python
    version: "3.11"

build:
  commands:
    - pip install -r requirements.txt

run:
  command: gunicorn app:app --bind 0.0.0.0:${PORT:-5000}
  port: 5000

persistentPaths:
  - /data
  - /data/uploads
  - /data/instance